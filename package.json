{
  "name": "Tweetron",
  "version": "1.4.2",
  "description": "",
  "main": "main.js",
  "dependencies": {
    "open": "^8.0.5"
  },
  "devDependencies": {
    "electron": "^15.3.0",
    "electron-packager": "^15.1.0",
    "eslint": "^8.2.0"
  },
  "engines": {
    "node": ">=14"
  },
  "scripts": {
    "start": "electron .",
    "package": "npm run clean && npm run build",
    "clean": "rm -rf Tweetron-*",
    "build": "electron-packager . Tweetron --platform=darwin --arch=x64 --extend-info=Info.plist --overwrite",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
