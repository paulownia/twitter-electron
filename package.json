{
  "name": "Tweetron",
  "version": "3.0.0",
  "description": "",
  "main": "dist/main.js",
  "type": "module",
  "dependencies": {
    "electron-context-menu": "^4.1.0",
    "open": "^8.0.5"
  },
  "devDependencies": {
    "@electron/packager": "^18.3.6",
    "@eslint/js": "^9.9.0",
    "@stylistic/eslint-plugin-js": "^2.7.2",
    "dotenv": "^16.3.1",
    "electron": "^34.0.1",
    "eslint": "^9.9.0",
    "globals": "^15.9.0",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "compile": "tsc && cp -r lib/preload dist/lib",
    "start": "npm run compile && electron .",
    "package": "npm run clean && npm run build",
    "clean": "rm -rf dist",
    "build": "npm run compile && node build.js",
    "lint": "eslint .",
    "format": "eslint --fix .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
