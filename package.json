{
  "name": "twitter-electron",
  "version": "1.1.0",
  "description": "",
  "main": "main.js",
  "dependencies": {},
  "devDependencies": {
    "electron": "^12.0.0",
    "electron-packager": "^15.1.0",
    "eslint": "^5.16.0"
  },
  "engines": {
    "node": ">=14"
  },
  "scripts": {
    "start": "electron .",
    "package": "npm run clean && npm run build",
    "clean": "rm -rf electric-twitter-*",
    "build": "electron-packager . electric-twitter --platform=darwin --arch=x64 --extend-info=Info.plist --overwrite",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
