{
  "name": "twitter-electron",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "dependencies": {
    "electron-default-menu": "^1.0.1"
  },
  "devDependencies": {
    "electron": "^8.2.3",
    "electron-packager": "^14.2.1",
    "eslint": "^5.16.0"
  },
  "scripts": {
    "start": "electron .",
    "package": "npm run clean && npm run build",
    "build": "electron-packager . electric-twitter --platform=darwin --arch=x64 --extend-info=Info.plist --overwrite",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
